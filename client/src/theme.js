import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2',
    },
    secondary: {
      main: '#dc004e',
    },
    tertiary: {
      main: "#FF5722"
    }
  },
  typography: {
    h1: {
      fontSize: '2.5rem',
    },
  },
});
